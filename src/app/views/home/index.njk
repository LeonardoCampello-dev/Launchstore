{% extends "../layout.njk" %}

{% block content %}

<h2>Últimos produtos</h2>

<section class="container shop">

    <div class="items">

    {% for product in products %}

        <div class="product">
            <div 
                class="image"
                style="background: url({{ product.img if product.img else '//placehold.it/500x500?text=Produto sem foto' }})"
            >

            <p>{{ product.price }}</p>

            <h4>{{ product.name }}</h4>

            <a href="/products/{{ product.id }}" class="button">Ver produto</a>

            </div>
        </div>

    {% endfor %}

    </div>

</section>


{% endblock content %}